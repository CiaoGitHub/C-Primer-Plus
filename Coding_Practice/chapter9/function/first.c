/*#include <stdio.h>
double min(double x, double y);
int main(void)
{
    double a, b;
    printf("enter two number to compare:");
    scanf("%lf %lf", &a, &b);
    printf("lower is %lf.\n", min(a, b));

    return 0;
}

double min(double x, double y)
{
    return (x < y ? x : y);
}
*/
