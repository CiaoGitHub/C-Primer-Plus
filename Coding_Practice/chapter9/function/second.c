/*#include <stdio.h>
void chline(char ch, int i, int j);
int main(void)
{
    char ch;
    int x, y;
    printf("enter an char and two number:\n");
    scanf("%c %d %d", &ch, &x, &y);
    chline(ch, x, y);

    return 0;

}

void chline(char ch, int i, int j)
{
    int a, b;
    for (a = 1; a <= j; a++)
    {
        for (b = 1; b <= i; b++)
            putchar(ch);
        printf("\n");
    }
}
*/
